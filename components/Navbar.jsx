import React from 'react';

const Navbar = () => {
  return (
  <div className="nav-container">
    <div className="nav-logo">Logo!</div>
    <div className="nav-right">
    <button>{zapas.cardsName}</button>
    </div>
  </div>
  );
};

export default Navbar;
